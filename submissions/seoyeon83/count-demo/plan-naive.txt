terraform_data.by_count[0]: Refreshing state... [id=6ecd9743-35ab-fab5-0860-471764e08d4e]
terraform_data.by_count[2]: Refreshing state... [id=bf28001b-ad0a-c32d-99d5-8023b902847c]
terraform_data.by_count[1]: Refreshing state... [id=56ddf300-2026-74dc-4e28-a4457f4a94e8]

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create
  - destroy

Terraform will perform the following actions:

  # terraform_data.by_count[0] will be destroyed
  # (because resource does not use count)
  - resource "terraform_data" "by_count" {
      - id               = "6ecd9743-35ab-fab5-0860-471764e08d4e" -> null
      - input            = "alpha" -> null
      - output           = "alpha" -> null
      - triggers_replace = "alpha" -> null
    }

  # terraform_data.by_count[1] will be destroyed
  # (because resource does not use count)
  - resource "terraform_data" "by_count" {
      - id               = "56ddf300-2026-74dc-4e28-a4457f4a94e8" -> null
      - input            = "bravo" -> null
      - output           = "bravo" -> null
      - triggers_replace = "bravo" -> null
    }

  # terraform_data.by_count[2] will be destroyed
  # (because resource does not use count)
  - resource "terraform_data" "by_count" {
      - id               = "bf28001b-ad0a-c32d-99d5-8023b902847c" -> null
      - input            = "charlie" -> null
      - output           = "charlie" -> null
      - triggers_replace = "charlie" -> null
    }

  # terraform_data.by_count["alpha"] will be created
  + resource "terraform_data" "by_count" {
      + id               = (known after apply)
      + input            = "alpha"
      + output           = (known after apply)
      + triggers_replace = "alpha"
    }

  # terraform_data.by_count["bravo"] will be created
  + resource "terraform_data" "by_count" {
      + id               = (known after apply)
      + input            = "bravo"
      + output           = (known after apply)
      + triggers_replace = "bravo"
    }

  # terraform_data.by_count["charlie"] will be created
  + resource "terraform_data" "by_count" {
      + id               = (known after apply)
      + input            = "charlie"
      + output           = (known after apply)
      + triggers_replace = "charlie"
    }

Plan: 3 to add, 0 to change, 3 to destroy.

─────────────────────────────────────────────────────────────────────────────

Note: You didn't use the -out option to save this plan, so Terraform can't
guarantee to take exactly these actions if you run "terraform apply" now.
