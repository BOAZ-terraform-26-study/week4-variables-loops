# Week 4. 변수 & 반복 (`for_each` vs `count`)

### [개념] 오늘의 목표
하드코딩을 걷어내고 `variables` / `locals` / `outputs`를 도입한다. 그리고 서브넷을 **map으로 `for_each`** 반복 생성한다. EC2는 1대로 유지.

### [실습] 1. 변수 정의
`week4/variables.tf`:
```hcl
variable "project" {
  type    = string
  default = "boaz-w4"
}

variable "vpc_cidr" {
  type    = string
  default = "10.0.0.0/16"
}

variable "subnets" {
  type = map(object({
    cidr = string
    az   = string
  }))
  default = {
    public-a = { cidr = "10.0.1.0/24", az = "ap-northeast-2a" }
    public-c = { cidr = "10.0.2.0/24", az = "ap-northeast-2c" }
  }
}
```

### [실습] 2. locals + provider + VPC
`week4/main.tf`:
```hcl
terraform {
  required_version = ">= 1.9"
  required_providers { aws = { source = "hashicorp/aws", version = "~> 6.0" } }
}
provider "aws" { region = "ap-northeast-2" }

locals {
  common_tags = {
    Project   = var.project
    ManagedBy = "terraform"
  }
}

resource "aws_vpc" "main" {
  cidr_block = var.vpc_cidr
  tags       = merge(local.common_tags, { Name = "${var.project}-vpc" })
}
```

### [실습] 3. ⭐ `for_each`로 서브넷 여러 개
```hcl
resource "aws_subnet" "public" {
  for_each          = var.subnets                 # map 반복
  vpc_id            = aws_vpc.main.id
  cidr_block        = each.value.cidr
  availability_zone = each.value.az
  tags = merge(local.common_tags, { Name = "${var.project}-${each.key}" })
}
```

### [개념] ⭐ 왜 `count`가 아니라 `for_each`인가 (오늘의 핵심)
`count`는 **리스트 인덱스**로 관리한다 → `aws_subnet.public[0]`, `[1]`.
가운데 항목을 지우면 인덱스가 밀려서 **뒤 리소스가 전부 재생성**된다(파괴적!).
`for_each`는 **map의 key**로 관리한다 → `aws_subnet.public["public-a"]`.
key로 고정되므로 하나를 지워도 나머지는 **그대로**.

```
count:    [0]=A  [1]=B  [2]=C      B 삭제 →  [0]=A [1]=C  (C가 이동=재생성!)
for_each: ["a"]=A ["b"]=B ["c"]=C  b 삭제 →  ["a"]=A ["c"]=C  (C 그대로)
```

### [실습] 4. EC2 1대 + outputs
```hcl
data "aws_ami" "al2023" {
  most_recent = true
  owners      = ["amazon"]
  filter { name = "name", values = ["al2023-ami-*-x86_64"] }
}

resource "aws_instance" "web" {
  ami           = data.aws_ami.al2023.id
  instance_type = "t3.micro"
  subnet_id     = aws_subnet.public["public-a"].id   # map key로 참조
  tags          = merge(local.common_tags, { Name = "${var.project}-web" })
}
```
`week4/outputs.tf`:
```hcl
output "subnet_ids" {
  value = { for k, s in aws_subnet.public : k => s.id }
}
output "instance_id" {
  value = aws_instance.web.id
}
```
```bash
terraform init && terraform apply
```

### [확인] 체크포인트
- `terraform state list`에 `aws_subnet.public["public-a"]`, `["public-c"]` 두 항목
- apply 끝에 `Outputs:`로 `subnet_ids` map과 `instance_id` 출력

### [관찰 ✍️] for_each 체감
`variables.tf`의 `subnets`에서 `public-a`를 삭제하고 `terraform plan`:
- plan 결과에 `public-c`가 재생성(replace)되는가? ___ (안 되면 for_each의 장점 체감 성공)

### [함정] `Invalid for_each argument`
`for_each`에는 map 또는 set만 가능. 리스트를 쓰려면 `toset(var.list)`로 변환.

### [정리] destroy 체크리스트
```bash
terraform destroy
```
- [ ] 서브넷 2개 + VPC + EC2 모두 삭제
- [ ] `terraform output` 비어있음
