# Week4 실습 — 변수화 + for_each
```bash
cp example.tfvars terraform.tfvars
terraform init && terraform apply
terraform output
terraform destroy
```
L1: TODO(variables/locals/network/outputs) 완성. L3-⭐: object 타입 + validation.
막히면 `../solution/`.
